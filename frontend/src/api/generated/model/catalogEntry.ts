/**
 * Generated by orval v8.3.0 🍺
 * Do not edit manually.
 * Mission Control API
 * OpenAPI spec version: 0.1.0
 */

/**
 * A single model in the dynamic catalog.
 */
export interface CatalogEntry {
  /** Canonical 9router/... model identifier. */
  model_id: string;
  /** Human-readable display name. */
  display_name: string;
  /** Top-level provider, e.g. 9router. */
  provider_family: string;
  /** Routing sub-family, e.g. ag, cx. */
  routing_family: string;
  /** Model family, e.g. gemini, claude, gpt. */
  family: string;
  /** Cost/capability tier: premium | specialized | balanced | budget. */
  tier: string;
  /** Primary capability: reasoning | general | coding | fast. */
  capability_class: string;
  /** Roles this model is suited for: lead, worker, coding, automation, review. */
  role_fit?: string[];
  /** Can be used as primary model. */
  supports_primary?: boolean;
  /** Can be used in fallback chain. */
  supports_fallback?: boolean;
  /** active | deprecated | unavailable */
  status?: string;
  /** Suggested fallback model_ids for this model. */
  fallback_candidates?: string[];
}
